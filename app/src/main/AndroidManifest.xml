<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.SchedulingApp"
        tools:targetApi="31">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity
            android:name=".BusinessSide.BusinessNavBar"
            android:exported="false"
            android:label="@string/title_activity_business_nav_bar" />
        <activity
            android:name=".BusinessSide.BusinessSignUpActivity"
            android:exported="false" />
        <activity
            android:name=".BusinessSide.BusinessLoginActivity"
            android:exported="false" />
        <activity
            android:name=".UserSide.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".UserSide.NavBarActivity"
            android:exported="false" />
        <activity
            android:name=".UserSide.SignUpActivity"
            android:exported="false" />
    </application>

</manifest>
